# AgentOre v0.0.10

This release makes pending-token recovery explicit when Codex revises an account's lifetime usage below the last accepted onchain baseline.

- shows `0` instead of an ambiguous dash;
- displays the exact deficit that must recover;
- pauses automatic submission and disables manual submission while the counter is below baseline;
- preserves the contract's monotonic cumulative-counter requirement.
